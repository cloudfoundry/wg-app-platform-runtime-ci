---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: ((repository))
    tag: ((tag))

inputs:
- name: ci
- name: repo

outputs:
- name: built-binaries

params:
  DEFAULT_PARAMS:
  FUNCTIONS:
  MAPPING:

run:
  path: ci/shared/tasks/build-binaries/task.bash
