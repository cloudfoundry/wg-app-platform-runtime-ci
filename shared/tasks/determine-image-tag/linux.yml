---
platform: linux

inputs:
- name: repo
  optional: true
- name: package-release
  optional: true
- name: ci

outputs:
- name: image_tag

params:
  GO_VERSION_FILE: ci/go_version.json
  IMAGE: cloudfoundry/tas-runtime-build
  DOCKER_REGISTRY_USERNAME: ((dockerhub-tasruntime/user))
  DOCKER_REGISTRY_PASSWORD: ((dockerhub-tasruntime/password))
  PLUGIN:

run:
  path: ci/shared/tasks/determine-image-tag/task.bash
